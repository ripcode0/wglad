@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/wglad-targets.cmake")

set(RELATIVE_PATH "${CMAKE_CURRENT_LIST_DIR}/../../../include")

get_filename_component(wglad_INCLUDE_DIRS ${RELATIVE_PATH} ABSOLUTE)

set(engine_LIBRARIES wglad::wglad)

set(wglad_FOUND TRUE)